# Copy to `.env.local` and adjust if your game server is not on port 3001.
# Next.js loads `.env.local` automatically; committed defaults are not required in development
# (`resolveGameWebSocketUrl` falls back to ws://localhost:3001 when NODE_ENV=development).
NEXT_PUBLIC_WS_URL=ws://localhost:3001
